import Model from "dva-core";
import {userInfoModel} from "../api/login";

export interface IState {

}

const initialState:IState = {

};

export default {
    namespace: 'bookmarkIndex',
    state: initialState,
    reducers: {

    },
    effects: {

    }
} as Model
